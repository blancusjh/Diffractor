"""Mathematical utilities: grids, Fourier operators, sampling and backends.

Grid construction, the (I)FFT / (I)FT operators, Fresnel sampling criteria and
CPU/GPU array-module resolution. Named ``mathutils`` (not ``math``) to avoid
shadowing the standard-library module.
"""
